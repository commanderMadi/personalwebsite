---
const { title, customClass } = Astro.props
---

<div class='w-full md:max-w-9/10 md:mx-auto lg:max-w-4xl p-6'>
  <h1 class={`${customClass || 'section-title'} text-4xl font-bold`}>{title}</h1>
  <slot />
</div>
